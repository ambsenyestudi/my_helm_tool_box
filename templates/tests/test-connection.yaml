apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-first-application.fullname" . }}-test-connection"
  labels:
    {{- include "my-first-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-first-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
