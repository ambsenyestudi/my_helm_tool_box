apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-first-application.fullname" . }}
  labels:
    {{- include "my-first-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-first-application.selectorLabels" . | nindent 4 }}
